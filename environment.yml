name: eql_source_layouts
channels:
    - conda-forge
dependencies:
    - python=3.7
    - pip
    - numpy
    - verde=1.3.0
    - harmonica=0.1.0
    # Other dependencies
    - matplotlib
    - ipython
    - jupyter
    - pyproj
    - jupytext
    # Development dependencies
    - pytest
    - pytest-cov
    - coverage
    - black
    - pylint
    - flake8
